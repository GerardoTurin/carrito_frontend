import { createTheme } from "@mui/material";


const theme = createTheme({
    palette: {
        primary: {
            main: "#002884",
        },
        secondary: {
            main: "#fff",
        },
        danger: {
            main: "#f44336",
        },
    },
});

export default theme;